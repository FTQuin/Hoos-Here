import Image from "next/image";
import {NextPage} from "next";

const Footer: NextPage = () => {
    return (
        <footer className = "mt-auto p-3 bg-orange-600 text-white text-center" >
            <p>Contact Us </p>
        </footer>
    );
}

export default Footer;